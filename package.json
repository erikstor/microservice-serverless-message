{
  "name": "aws-node-express-api-project",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "env-cmd -f '.env' --fallback jest --collectCoverage --detectOpenHandles --maxWorkers=4 "
  },
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "serverless-http": "^3.1.1",
    "twilio": "^4.11.1"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^12.0.4",
    "supertest": "^6.3.3"
  }
}
